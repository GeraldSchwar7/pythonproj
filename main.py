import tkinter as tk

# Create the main window
window = tk.Tk()

# Window title
window.title("Interpreter")

# Starting size
window.geometry("800x600")

# Allow resizing (enabled by default, but explicit here)
window.resizable(True, True)

# Start the application
window.mainloop()